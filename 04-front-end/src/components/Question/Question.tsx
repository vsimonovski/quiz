import React from 'react';
import * as S from './Question.style';

interface QuestionProps {
    currentQuestion: number;
}

const Question = ({currentQuestion}: QuestionProps) => {
    return <S.Container></S.Container>;
};

export default Question;
